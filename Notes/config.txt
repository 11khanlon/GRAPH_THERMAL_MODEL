Remaining Questions: 
this is the config file: 
Start with the values they used [Ti-64] to validate your math and model works. Then branch out to inconel


build dictionary: 
do you segment the substrate too? how to you do it here? is it included in deposition? 
Need to extract CAD values instead of manually listing them each time [length, width, height, number of layers, substrate length, etc]
nodes.py inserts .stl file, but does it work with the length, width suttf in the dictionary given

NODES = {
    "seed": 42,
    "random": True}
np.random.seed(NODES["seed"])
are these coordinates? If the nodes are placed randomly, how is the node density assumed constant? [seed]
Isn't this established in the node sectoin

do they calculate h_eff? why do the simplify it later  [radiation?]. Where is convection values incorporated
I do not want to model the clamp. How will this mess up the simulation? I only want one dwell time also how will this affect other code

-------------------------------------------------
Based on:

Riensche et al.
Thermal Modeling of Directed Energy Deposition Additive Manufacturing
Using Graph Theory
Rapid Prototyping Journal (2023)

**Variables you can edit:**

Material Properties:
MATERIAL = {} python dictionary 
key : value 

Laser Parameters:
LASER = {}  
Another dictionary for machine parameters

Build Geometry: 
BUILD = {}

Graph Parameters:
GRAPH = {}


Block Discretization: 
BLOCK = {}
how to break up .stl file
does it discretize only the wall? or the substrate as well? should you include substrate in the .stl file 

Convection:
Forced convection --> argon assited cooling. How does this apply to each side
Free convection --> natural air cooling
clamp convection --> do we need a clamp?

why don't include radiation? 
do they calculate h_eff? why do the simplify it later 

Dwell Time: 
DWELL = {} 
the waiting time between deposited layers 

Random node settings: 
NODES = {
    "seed": 42,
    "random": True
}

This controls how graph nodes are generated 
with a seed every run gives the exact same sequence

Example:
seed = 42
every run gives: 
0.3745
0.9507
0.7319
...
are these coordinates? If the nodes are placed randomly, how is the node density assumed constant? 

do you model the substrate too? 



-----------------------------
Ti-64 

#"thermal_conductivity": 7.2,    # k, W/m-K,  
"specific_heat": 560,         # cp,  J/kg-K'''

-----------------------------
Inconel Model Values

MATERIAL = {

    "density": 8190,              # rho, kg/m^3
    "thermal_conductivity": 18.0,    # k, W/m-K
    "specific_heat": 550,         # cp,  J/kg-K
    "ambient_temperature": 298.15,    #  ambient temperature, K
    "liquidus_temperature": 1609.0    # liquidus temperature, K
}

LASER = {

    "power": 1070.0,            # Watts
    "scan_speed": 12.7e-3,      # m/s
    "beam_diameter": 1.778e-3,  # meters
    "layer_height": 0.381e-3,   # meters
    "hatch_width": 1.143e-3,    # meters
    "meltpool_temperature": 2000  # Goldak meltpool temperature, must be above liquidus and fully molten temperature

}

